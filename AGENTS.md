# Project instructions

Read `docs/project-context.md` first, then the product, domain, standards, and assumptions documents it links.

Keep `src/domain/` deterministic and UI-independent. Treat unknown capabilities as data, never as false or maximum capability. Do not infer capability from connector shape or specification version labels.

Update the cockpit and relevant decision/source records whenever scope, evidence, or a diagnostic rule changes.
